[package]
name = "dataflow-mcp"
version = "1.0.2"
edition = "2021"
description = "MCP server for DataFlow: generate manifests and migrate Kafka Connect to DataFlow"
license = "MIT"

[dependencies]
rmcp = { version = "1.0.2", features = ["server", "transport-io", "schemars"] }
tokio = { version = "1.0.2", features = ["full"] }
serde = { version = "1.0.2", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
schemars = { version = "1.0.2", features = ["derive"] }
thiserror = "2"
